general:
  artifacts:
    - "build/reports/jacoco"

machine:
  java:
    version: oraclejdk8

test:
  override:
    - ./gradlew clean test

deployment:
  production:
    branch: master
    commands:
      - ./gradlew release publishPlugins tag
